'use client'

export function DocumentStats() {
  return (
    <div className="gc-panel p-6 mt-8">
      <h2 className="text-lg font-semibold text-[var(--gc-ink)] font-display mb-2">Document Statistics</h2>
      <p className="text-sm text-[var(--gc-muted)]">
        Statistics will appear here after crawls complete.
      </p>
    </div>
  )
}
